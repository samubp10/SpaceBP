<div class="sign-buttons">
    <a href="{{ Request::root() }}/{{ app()->getLocale() }}/register" id="sign-up">{{ __('Sign up') }}</a>
    <a href="{{ Request::root() }}/{{ app()->getLocale() }}/login" id="sign-in">{{ __('Sign in') }}</a>
</div>
